# Milestone 6: Functions
function add(a, b):
    return a + b

function greet(name):
    return "Hello, " + name + "!"

say add(2, 3)
say greet("Plainly")

set numbers = [1, 2, 3]
say numbers[0]
say numbers[1 + 1]

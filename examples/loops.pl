# Milestone 5: Loops
set names = ["Aisha", "Ben", "Carlos"]

for each name in names:
    say "Hello " + name

repeat 3 times:
    say "Loop!"

set greeting = "abc"
for each letter in greeting:
    say letter

say length(names)
